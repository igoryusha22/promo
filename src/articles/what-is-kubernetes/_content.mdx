По запросу в google, можно получить следующий ответ:

> Kubernetes — открытое программное обеспечение для оркестровки контейнеризированных приложений — автоматизации их развёртывания, масштабирования и координации в условиях кластера. Поддерживает основные технологии контейнеризации, включая Docker, rkt, также возможна поддержка технологий аппаратной виртуализации.

Если попробовать ответить более развернуто, что он делает и не делает, можно выделить основные пункты.

**Kubernetes делает следующее:**

- Создает, наблюдает, и убивает контейнеры
- Осуществляет контроль состояния, автоматические развертывания и откаты реплик контейнеров внутри узлов кластера Kubernetes
- Осуществляет распределение нагрузки между узлами кластера Kubernetes
- Предоставляет автоматическое монтирование систем хранения для контейнеров
- Предоставляет декларативный API и CLI для управления
- И еще множество полезных, и не очень, модулей и сервисов, которые можно развернуть для управления автоматизацией, инфраструктурой и контейнерами

**Kubernetes не делает следующее:**

- Не собирает контейнеры с исходным кодом вашего приложения или сервиса
- Не предоставляет процессы и решения непрерывной интеграции (CI)
- Не включает в себя решения и системы сбора журналов и метрик
- Не включает в себя решения и системы хранения данных
- Не включает в себя решения и системы хранения контейнеров (registry)
- Не включает в себя решения и системы от всех бед и болячек инфраструктуры

## Компоненты и архитектура

Kubernetes измеряется в кластерах, то есть, так же как windows измеряется в компьютерах.

Кластер состоит компонентов, которые в свою очередь тоже состоят из компонентов.

Если вывести обычный маленький кластер, то он будет выглядить следующим образом:

- cluster
  - master node (cp, control plane) - управляет worker нодами
    - kube-apiserver - предоставляет API кубера, с помощью которого, каждый компонент общается друг с другом
    - etcd - распределенное ключ-значение хранилище для всех данных кластера
    - kube-scheduler - планирует размещение подов на узлах кластера
    - kube-controller-manager - запускает контроллеры
    - kubelet - сервис или агент, который контролирует запуск основных компонентов (контейнеров) кластер
  - worker node – используется для запуска ващих приложений
    - kube-proxy - конфигурирует правила сети на узлах
    - kubelet - сервис или агент, который контролирует запуск компонентов (контейнеров) кластера

> Если объяснять на пальцах, что такое kubernetes кластер, то это стройка, на которой есть люди (сервера), среди которых есть прораб (мастер нода), который управляют обычным строителем (мастер нода).

Master и worker нод может быть несколько, и обычно их несколько, на случай, если какая-то отвалится, то подстрахует другая. Таким образом будет гарантирована отказоустойчивость.

## Сущности

### Pod

Базовый строительный блок Kubernetes, представляющий собой группу из одного или нескольких контейнеров приложения. Чаще всего по своей сути является под = контейнер.

### Controller

####

### Су
